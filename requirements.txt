# WPS 自动打卡服务 - 依赖列表
# 安装命令: pip install -r requirements.txt

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 异步 HTTP 客户端（用于调用 WPS API）
httpx>=0.25.0

# 异步 SQLite（数据库）
aiosqlite>=0.19.0

# 浏览器自动化（打卡核心）
playwright>=1.40.0

# 定时任务
apscheduler>=3.10.0

# HTTP 请求（用于通知）
requests>=2.31.0

# 数据验证（FastAPI 依赖）
pydantic>=2.5.0
